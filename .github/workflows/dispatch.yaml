name: "Dispatch 🚚"
on:
  workflow_dispatch:

jobs:
  build:
    name: "Build"
    uses: "./.github/workflows/build.yaml"
    permissions:
      id-token: write
      pages: write
    with:
      repo_upload: false
