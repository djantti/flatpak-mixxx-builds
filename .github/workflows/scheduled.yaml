name: "Scheduled ⏰"
on:
  schedule:
    - cron: "0 6 * * *"

jobs:
  build:
    name: "Build"
    uses: "./.github/workflows/build.yaml"
    permissions:
      id-token: write
      pages: write
    with:
      repo_upload: false
